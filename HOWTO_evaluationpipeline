## What is required to use the evaluation pipeline

### Run everything from genforce folder

Reqs:
1. run 'bash weights/download_weights.sh'
            ... to download the weights of cnn detection

2. run 'pip install -r requirements.txt'
            ... to install the requirements for the cnn detection

3. use 'bsub -R "rusage[mem=20480,ngpus_excl_p=1]" -R "select[gpu_mtotal0>=10240]" -W 4:00 scripts/dist_train.sh <lamb> <metric> <baseLR> <n_images>'
            ... to start an evaluation of a combination of parameters
            ... bsub -R "rusage[mem=20480,ngpus_excl_p=1]" -R "select[gpu_mtotal0>=10240]" -W 2:00 scripts/dist_train.sh 0.1 0.1 0.1 10000

Make sure to
- Not evaluate one generator twice
